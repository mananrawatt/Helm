apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-login-service
  labels:
    app: login-app

spec:
  type: {{ .Values.service.type }}

  selector:
    app: login-app

  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
