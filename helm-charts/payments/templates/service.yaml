apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-payments-service
  labels:
    app: payments

spec:
  type: {{ .Values.service.type }}

  selector:
    app: payments

  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
