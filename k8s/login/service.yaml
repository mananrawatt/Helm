apiVersion: v1
kind: Service

metadata:
  name: login-service
  labels:
    app: login-app

spec:
  type: ClusterIP

  selector:
    app: login-app

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
