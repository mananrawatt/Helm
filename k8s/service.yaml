apiVersion: v1
kind: Service

metadata:
  name: helm-learning-service
  labels:
    app: helm-learning-app

spec:
  type: ClusterIP

  selector:
    app: helm-learning-app

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
